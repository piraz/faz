{{# dropdown }}
    {{# ../isRoot }}
    <li class="nav-item dropdown">
        <a class="nav-link active dropdown-toggle" data-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">{{ ../content }}</a>
        <ul class="dropdown-menu">
            {{#each ( ../items, child=value)}}
                {{ child.html }}
            {{/each}}
        </ul>
    </li>
    {{else}}
    <li class="dropdown-submenu">
        <a class="dropdown-item dropdown-toggle" href="#">{{ ../content }}</a>
        <ul class="dropdown-menu">
            {{#each (../items, child=value)}}
                {{ child.html }}
            {{/each}}
        </ul>
    </li>
    {{/ ../isRoot }}
{{else}}
    {{# isRoot }}
    <li class="nav-item">
        <a id:from="../id" class:from="../class" on:click="../activate()" target:from="../target" href:from="../getHref()" aria-controls:from="../ariaControls" aria-selected:from="../ariaSelected">{{ ../content }}</a>
    </li>
    {{else}}
    <li>
        <a id:from="id" class="dropdown-item" role="tab" target:from="target"  href:from="getHref()">{{ content }}</a>
    </li>
    {{/ isRoot }}
{{/ dropdown }}
