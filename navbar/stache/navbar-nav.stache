{{# brand }}
    {{ this.html }}
{{/ brand }}
<div class="collapse navbar-collapse" id="navbarNavAltMarkup">
    <div class="navbar-nav">
        {{#each (items, item=value)}}
            {{ item.html }}
        {{/each}}
    </div>
</div>
