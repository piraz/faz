{{# this.isLoading}}
    <div>Loading...</div>
{{ else }}
    <nav id:from="id" class:from="class">
        <div class="container-fluid">
            {{#items}}
                {{{ this.html }}}
            {{/items}}
        </div>
    </nav>
{{/ this.isLoading}}
